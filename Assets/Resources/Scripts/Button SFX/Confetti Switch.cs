using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class ConfettiSwitch : MonoBehaviour
{
    //private static GameObject instance;

    //void Awake()
    //{

    //    if (instance == null)
    //    {
    //        instance = this;
    //        DontDestroyOnLoad(gameObject); // Don't destroy this GameObject when loading a new scene

    //    }
    //}

    //private static GameObject instance;

    //void Awake()
    //{

    //    if (instance == null)
    //    {
    //        instance = gameObject;  // Assign the GameObject, not the script
    //        DontDestroyOnLoad(instance); // Don't destroy this GameObject when loading a new scene
    //        Debug.Log("ConfettiSwitch instantiated and marked as DontDestroyOnLoad.");

    //    }
    //    else
    //    {
    //        Debug.Log("ConfettiSwitch already exists. Not instantiating a new one.");

    //    }
}


